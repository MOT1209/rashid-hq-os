# خطة العمل — Alking HQ OS

> آخر تحديث: 2026-08-26 · آخر commit: `4a7a090` · الإنتاج: https://rashid-hq-os.vercel.app

هذه الوثيقة نقطة الاستئناف. تقرأها وتعرف: أين وصلنا، ما الذي يعمل فعلاً، ما الذي ما زال مكسورًا، وما الخطوة التالية ولماذا.

---

## 1. أين وصلنا

### نظام الوكلاء — يعمل ومُثبت بالدليل
وكلاء الأقسام كانوا **نصًا في برومبت بلا منفّذ**. الآن صاروا حقيقيين:

- كل قسم في جدول `departments` له `system_prompt` و `model` (migration `0011`)
- الكونسول يفوّض عبر أداة `delegate_to_department`
- وكيل القسم ينفّذ بنفس سجل الأدوات، ويسجّل باسمه (`agent_name`)
- **الدليل**: أول سجل في تاريخ المشروع باسم `Dev Agent` مع `list_projects` بحالة `success`

الأثر الملموس: صفحات الأقسام كانت تعرض «لا توجد نشاطات بعد» **إلى الأبد** — لأن لا شيء يسجّل بأسمائهم. الآن تضيء.

### ما أُنجز قبل ذلك في هذه الجلسة
| الميزة | الحالة |
|---|---|
| تنبيه إيميل عند فشل مشروع 3 مرات متتالية | الكود يعمل — **لكن لا يصل** (انظر §2) |
| إعادة محاولة في الفحص الصحي (تقلل الإنذارات الكاذبة) | ✅ |
| تنبيه تفعيل 2FA لحساب المالك | ✅ |
| تصدير CSV لسجل النشاط | ✅ |
| بحث سريع عن المشاريع (يدعم تطبيع العربية) | ✅ |
| تسجيل عمليات الدخول | ✅ |
| تحسينات صفحة القسم (KPIs، جدول تفاعلي، فلترة، إضافة مشروع، آخر فحص صحي، تبويبات) | ✅ |
| 18 مهارة وكلاء من skills.sh | ✅ |

### التحقق الحالي
`typecheck` ✅ · `lint` ✅ · `build` ✅ · `vitest` ✅ **167/167** (22 ملف)

---

## 2. ما هو مكسور أو ناقص الآن

### أ. تنبيهات الفشل مبنية لكنها صامتة ⚠️
تكامل Resend حالته **`Onboarding`** — دومين `rashid-wep.com` غير مُتحقّق منه.
النتيجة: كود التنبيه يعمل، لكن البريد لا يُسلَّم فعليًا. **ميزة موجودة على الورق فقط.**

**الحل:** إضافة سجلات DNS (SPF/DKIM) عند مزوّد الدومين ثم التحقق في لوحة Resend. يحتاج وصولك لإعدادات DNS.

### ب. لا مشروع يملك `mcp_endpoint`
| المشروع | التصنيف | نقطة MCP |
|---|---|---|
| Demo Project | Web App | ✗ |
| conecter-app | Web App | ✗ |
| EDITOR.AI | Media | ✗ |
| Englisch-app | Mobile App | ✗ |

**الأثر:** الفحص الصحي يتجاهلها كلها، ووكلاء الأقسام لا يستطيعون تشغيل أي أداة بعيدة — أي أن `call_project_tool` (أقوى أداة عندنا) بلا هدف. الوكلاء حاليًا يقدرون على القراءة والإدارة فقط، لا التنفيذ على المشاريع.

### ج. `Demo Project` ما زال موجودًا
مشروع وهمي يشوّه إحصائيات اللوحة. يحتاج ضغطة تأكيد منك في `/dashboard/projects` (زر الحذف يستخدم `window.confirm` وهو يجمّد أتمتة المتصفح، فما أقدر أضغطه).

### د. برومبتات الأقسام تُعدَّل بـ SQL فقط
لا توجد واجهة لتحرير `system_prompt` أو `model`. **هذه ثغرة تصميمية من جانبي** — بنيت وكلاء قابلين للتخصيص بلا طريقة لتخصيصهم.

### هـ. مفتاح Groq مكشوف
مرّ في نص المحادثة. يجب إلغاؤه وإنشاء غيره.

---

## 3. الخطة — بالأولوية

### أولوية 1: واجهة تحرير الوكلاء (الأهم)
**لماذا أولاً:** نظام الوكلاء نصف قابل للاستخدام بدونها. تعديل شخصية وكيل يتطلب SQL على قاعدة الإنتاج — وهذا ليس منتجًا.

- صفحة أو لوحة (على الأرجح داخل `/dashboard/departments/[dept]` أو `/dashboard/settings`) لتحرير `system_prompt` و `model` لكل قسم
- إعادة استخدام نمط `ProjectRow` للتحرير المضمّن
- `requireAdmin` على الحفظ، وتسجيل التعديل في `agent_logs` مثل كل فعل مالك آخر
- تحقق: عدّل برومبت وكيل التطوير من الواجهة، فوّض له مهمة، ولاحظ تغيّر سلوكه

### أولوية 2: إعطاء الوكلاء شيئًا يشغّلونه
**لماذا:** بدون `mcp_endpoint` واحد على الأقل، `call_project_tool` عديمة الفائدة والوكلاء لا ينفّذون شيئًا على المشاريع.

خياران:
- **(أ)** تسجّل نقاط MCP الحقيقية لمشاريعك لو كانت جاهزة
- **(ب)** نبني نقطة MCP بسيطة في أحد مشاريعك (`conecter-app` مثلاً) — مهارة `mcp-builder` المثبّتة تغطي هذا بالضبط، والتطبيق نفسه يحوي نموذجًا عاملاً في `src/app/api/mcp/route.ts`

### أولوية 3: وكلاء مجدولة (المهام الدائمة)
**لماذا:** الوكلاء حاليًا لا يعملون إلا بطلب منك. الخطوة الطبيعية أن يكون لكل وكيل مهمة يومية دائمة.

- cron جديد في `vercel.json` يشغّل مهمة دائمة لكل قسم
- عمود `standing_task` على `departments`، فارغ = لا شيء مجدول
- إعادة استخدام `runDepartmentAgent` كما هي — المنفّذ جاهز
- **انتبه:** هذا يصرف رصيد نموذج يوميًا وبلا إشراف. لازم سقف واضح وإمكانية إيقاف لكل قسم

### أولوية 4: تفعيل تسليم البريد فعليًا
سجلات DNS لـ Resend، ثم اختبار حقيقي: افتعل فشل 3 مرات وتأكد أن الإيميل وصل.

### أولوية 5: تنظيف
- حذف `Demo Project`
- إلغاء مفتاح Groq وإنشاء بديل
- Sentry (تعثّر مرتين بعطل من جهة Vercel — ممكن نجرب Rollbar بديلاً)

---

## 4. معرفة مهمة لا تُفقد

هذه أشياء اكتُشفت بالتجربة وتوفّر وقتًا كبيرًا لاحقًا:

### المزوّدون — ثلاثة جُرِّبوا
| المزوّد | النتيجة |
|---|---|
| Vercel AI Gateway | ✗ يرفض كل طلب بلا بطاقة ائتمان (`customer_verification_required`) |
| Google Gemini | ✗ المفاتيح المتاحة كانت رموز `AQ.` مؤقتة تموت خلال دقائق، ومشروع بحصة صفر |
| **Groq** | ✅ مفتاح دائم، بلا بطاقة، واستدعاء أدوات يعمل |

النموذج الافتراضي `openai/gpt-oss-120b` — اختير لقدرته على استدعاء الأدوات تحديدًا (الكونسول يشغّل 14 أداة).

### فخ حقيقي: التحقق الصارم من مخطط الأدوات
Groq يرفض معاملات الأدوات إن لم تطابق المخطط بدقة، **والنموذج يرسل `null` لا `undefined`** للحقول الاختيارية. كل `.optional()` كانت تنفجر:
```
`/category`: expected string, but got null
```
الحل: `.nullish()` في كل مكان. **الاختبارات المحاكاة الـ167 كانت تمر في الحالتين** — لأنها لا تمر بتحقق المزوّد.

**الدرس:** لأي ميزة تعتمد على نموذج، شغّل اختبارًا حيًا مقابل النموذج والقاعدة الحقيقيين. المحاكاة لا تكشف هذه الفئة من الأخطاء.

### تفاصيل تقنية تُنسى
- `delegationDepth` في `ToolContext`: الكونسول 0، وكيل القسم 1، والأداة ترفض عند 1. بدونه: حلقة تفويض لا نهائية تحرق رصيدًا
- `"CEO Console"` في سجل النشاط **مصدران**: الذكاء الاصطناعي، و`OWNER_ACTOR` في `src/app/actions.ts:22` لأفعالك اليدوية. هذا خدعني أثناء التشخيص
- `.agents/skills` مستثناة من eslint — ملف مهارة خارجية يستخدم `require()` وكان يُفشل CI
- زر حذف المشروع يستخدم `window.confirm` — يجمّد أتمتة المتصفح، فالحذف يدوي

---

## 5. أول شيء غدًا

1. إلغاء مفتاح Groq وإنشاء بديل (دقيقة)
2. البدء في **أولوية 1: واجهة تحرير الوكلاء**

وإن أردت ترتيبًا مختلفًا، غيّر الأولويات هنا قبل أن نبدأ.
