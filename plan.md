# خطة العمل — Alking HQ OS

> آخر تحديث: 2026-09-13 · آخر commit كود: `5148a6a` (Insights + سطح المهام الدائمة + `defineTool`) · الإنتاج: https://rashid-hq-os.vercel.app

هذه الوثيقة نقطة الاستئناف. تقرأها وتعرف: أين وصلنا، ما الذي يعمل فعلاً، ما الذي ما زال مكسورًا، وما الخطوة التالية ولماذا.

---

## 1. أين وصلنا

### بوابة السياسة، طابور الموافقة وتصنيف الفاعل — مستوحاة من OpenBot (2026-09-12، commit `c801b35`)
المالك شاهد عرضًا لـ [CopilotKit/OpenBot](https://github.com/CopilotKit/OpenBot) (منصة وكلاء AI بحوكمة صريحة: بوابة سياسة توافق/ترفض كل إجراء قبل تنفيذه، وسجل تدقيق). طلب نقل **المفاهيم** لا التقنية (لا حاويات، لا CEL، لا AG-UI — هذا المشروع لا يشغّل كودًا حرًا لوكلائه). الخطة الكاملة في `.claude/plans/giggly-jingling-bubble.md`.

المُنفَّذ:
- **`src/lib/mcp/executor.ts`** (جديد) — نقطة تنفيذ واحدة (`runTool()`) توحّد حلقات التنفيذ الأربعة المكررة (الكونسول، وكيل القسم، ناقلا MCP)، وتسدّ ثغرتين تدقيق كانتا موجودتين فعلاً: الكونسول لم يكن يتحقق من الصلاحية إطلاقًا (كان يمنح نفسه القراءة+الكتابة دائمًا فيمر تلقائيًا)، ورفض الصلاحية على أي من ناقلي MCP لم يكن يُسجَّل في `agent_logs` أبدًا.
- **`src/lib/policy.ts`** (جديد) — قواعد `allow`/`deny`/`require_approval` مرتّبة، بافتراضي آمن لا يُخالف السلوك الحالي: أداة قراءة بلا قاعدة تُسمح (لا شيء تُسرّبه)، أداة كتابة بلا قاعدة تُحوَّل لموافقة بشرية (بدل أن ترث صلاحية كاملة كما كان يحدث، أو تُرفض بصمت). قاعدة دفاعية إضافية: أي كتابة من `standing_task_routine` (المهمة اليومية غير المراقبة) تُرفض عدا `call_project_tool`، فوق قائمة `AUTONOMOUS_TOOLS` الموجودة أصلاً في `department-agent.ts`.
- **`src/lib/approvals.ts`** (جديد) + migration `0015` — طابور موافقة بشرية حقيقي. `delete_project` (الإجراء الوحيد الذي لا رجوع فيه) يحتاج الآن موافقة مشرف من لوحة جديدة في `/dashboard/access` قبل أن يُنفَّذ فعليًا.
- **migration `0014`** — أعمدة `decision`, `decision_reason`, `actor_type` على `agent_logs` + حالة جديدة `awaiting_approval`.
- **تصنيف الفاعل** (`ActorType`): كل سجل نشاط يُوسم الآن بـ `person` / `agent_token` / `department_agent` / `standing_task_routine`.
- كشفتُ أثناء التنفيذ فجوة إضافية غير متعلقة بـ OpenBot: مؤشرات «قيد التنفيذ» في `/dashboard` وصفحة القسم كانت تحسب فقط `status = pending`، فلا تُظهر صفوف `awaiting_approval` الجديدة — أُصلح في `src/lib/queries.ts`.

**تحقق مُنجز**: 19 اختبار وحدة جديد (`tests/policy.test.ts`, `tests/approvals.test.ts`) + كامل الحزمة القديمة (277 اختبارًا) سليمة بلا تغيير سلوك. **تحقق متبقٍّ (يحتاج قاعدة بيانات حية):**
1. طبّق `supabase/migrations/0014_policy_decisions.sql` و `0015_tool_approvals.sql` — **لم يُطبَّقا على الإنتاج بعد**.
2. من توكن كتابة، استدعِ `delete_project` عبر `/api/mcp` → يجب أن يظهر «بانتظار الموافقة» بدل الحذف الفوري، وتراه في لوحة «طلبات بانتظار الموافقة» بـ `/dashboard/access`.
3. وافق عليه كمشرف → يُنفَّذ الحذف فعليًا. جرّب «رفض» على طلب آخر → المشروع يبقى.

### Insights + سطح المهام الدائمة + `defineTool` (2026-09-13، commit `5148a6a`)
آخر ثلاثة بنود كانت في قائمة «أول شيء غدًا». بهذا الـcommit صارت القائمة فارغة.

- **`/dashboard/insights`** — أول صفحة تقرأ `agent_runs` بدل أن تكتب فيه فقط: توكنات/يوم، تشغيلات، نسبة فشل، متوسط مدة، وتفصيل حسب النوع وحسب الوكيل على نافذة 30 يومًا. التجميع في `src/lib/insights.ts` كدالة نقية (`summariseRuns`) بنفس فصل `standing-tasks.ts`/`health-alert.ts`. الرسم البياني divs عادية — مكتبة رسوم لأجل عرض واحد كانت ستكون الاعتماد الخامس في هذا المشروع.
- **سطح المهام الدائمة (2.3)** — بطاقة على صفحة القسم تعرض تقرير آخر تشغيلة؛ كان يُقرأ سابقًا كـJSON خام في البثّ فقط. التحليل في `src/lib/standing-task-summary.ts` (نقي، يتحمّل صفًا بشكل قديم أو `result` فارغ).
- **بريد ملخّص المهام الدائمة (2.4)** — `sendStandingTaskDigest` في `email.ts`، يُرسل **رسالة واحدة للدفعة كلها** في نهاية الـcron لا رسالة لكل قسم. فشل الإرسال لا يُفشل الـcron.
- **`defineTool<S>()` (0.4)** — يستنتج وسائط كل أداة من مخططها بدل النوع المكتوب يدويًا بجانبه بلا أي تحقق متبادل — وهو الحدّ نفسه الذي عاش فيه عطل Groq-`null`. **الأدوات الـ16 انتقلت بصفر أخطاء نوعية**، وهو أول دليل فعلي أن الأنواع اليدوية كانت صحيحة.

### سجل `agent_runs` — تتبّع تكلفة التوكنات (2026-09-13، commit `fbe6e7e`)
البند 1.4 من تقرير المراجعة. لا رقم إنفاق واحد كان موجودًا رغم أن الوكلاء يصرفون رصيدًا يوميًا.

- **migration `0016`** — جدول `agent_runs`: صف واحد لكل استدعاء `generateText`/`streamText` (`agent_name`, `kind` [`console`|`delegation`|`standing_task`], `tokens_in/out`, `duration_ms`, `step_count`, `status`). لا عمود `run_id` منفصل — `id` (uuid) يؤدي هذا الدور.
- **`src/lib/agent-run.ts`** (جديد) — `recordAgentRun()`، آمنة عند الفشل (fire-and-forget) بنفس نمط `src/lib/activity.ts`.
- **`department-agent.ts`** — يلفّ `generateText` بـ try/catch: يسجّل الصف عند النجاح (توكنات من `result.usage`) وعند الفشل (قبل إعادة رمي الخطأ)؛ `kind` يُشتقّ من `mode` الموجودة أصلاً (`autonomous` → `standing_task`, غير ذلك → `delegation`) — **لا حاجة لتعديل `standing-tasks/route.ts` منفصلًا**، لأن `runDepartmentAgent` هي القاسم المشترك بين مسار التفويض ومسار المهمة الدائمة.
- **`console/route.ts`** — `streamText` غير متزامن (يُعاد الرد فورًا كبثّ)، فالتسجيل يحدث عبر خطافات `onStepFinish`/`onFinish`/`onError` بدل انتظار النتيجة مباشرة.

**لم يُبنَ بعد (عمدًا، خارج نطاق هذه الأولوية):** أي واجهة لعرض هذه البيانات — تلك أولوية غد 4 (`/dashboard/insights`) اللاحقة، وتعتمد على هذا الجدول.

**تحقق مُنجز:** 3 اختبارات جديدة لـ `recordAgentRun` + 3 اختبارات جديدة على `runDepartmentAgent` (kind الصحيح، النجاح يسجّل التوكنات/الخطوات، الفشل يسجّل ويعيد رمي الخطأ) — **302/302** إجمالاً. **تحقق متبقٍّ (قاعدة بيانات حية):** بعد تطبيق `0016`، شغّل محادثة كونسول ومهمة مفوَّضة وتأكد من ظهور صفوف في `agent_runs` بـ `kind` الصحيح وتوكنات غير صفرية.

### نظام الوكلاء — يعمل ومُثبت بالدليل
وكلاء الأقسام كانوا **نصًا في برومبت بلا منفّذ**. الآن صاروا حقيقيين:

- كل قسم في جدول `departments` له `system_prompt` و `model` (migration `0011`)
- الكونسول يفوّض عبر أداة `delegate_to_department`
- وكيل القسم ينفّذ بنفس سجل الأدوات، ويسجّل باسمه (`agent_name`)
- **الدليل**: أول سجل في تاريخ المشروع باسم `Dev Agent` مع `list_projects` بحالة `success`

الأثر الملموس: صفحات الأقسام كانت تعرض «لا توجد نشاطات بعد» **إلى الأبد** — لأن لا شيء يسجّل بأسمائهم. الآن تضيء.

### ما أُنجز قبل ذلك في هذه الجلسة
| الميزة | الحالة |
|---|---|
| تنبيه إيميل عند فشل مشروع 3 مرات متتالية | الكود يعمل — **لكن لا يصل** (انظر §2) |
| إعادة محاولة في الفحص الصحي (تقلل الإنذارات الكاذبة) | ✅ |
| تنبيه تفعيل 2FA لحساب المالك | ✅ |
| تصدير CSV لسجل النشاط | ✅ |
| بحث سريع عن المشاريع (يدعم تطبيع العربية) | ✅ |
| تسجيل عمليات الدخول | ✅ |
| تحسينات صفحة القسم (KPIs، جدول تفاعلي، فلترة، إضافة مشروع، آخر فحص صحي، تبويبات) | ✅ |
| 18 مهارة وكلاء من skills.sh | ✅ |

### التحقق الحالي
`typecheck` ✅ · `lint` ✅ (0 أخطاء) · `build` ✅ (Next 16 + Turbopack) · `vitest` ✅ **314/314** (35 ملف)
> الطقم الكامل بطيء على جهاز المالك (~40s ويُقتل أحيانًا). شغّل الملفات المتأثرة، أو `npx vitest run --reporter=dot`.

---

## 2. ما هو مكسور أو ناقص الآن

### أ. تنبيهات الفشل — تعمل للمالك عبر عنوان Resend الجاهز (قرار 2026-08-28)
`rashid-wep.com` بلا DNS zone إطلاقًا (`Non-existent domain`) وحالته في Resend `not_started`. إصلاحه يتطلب وصولًا للمُسجّل لا نملكه، **وقرار المالك: تجاهله.**

الحل المعتمد: `EMAIL_FROM` غير مضبوط → الكود يرجع لـ `onboarding@resend.dev` (انظر `src/lib/email.ts:24`). هذا يُسلّم **لعنوان صاحب حساب Resend فقط** — وهو كافٍ لمركز قيادة بمالك واحد.

**تحقق واحد متبقٍّ:** `OWNER_EMAILS` في Vercel يجب أن يحوي نفس إيميل حساب Resend، وإلا تفشل التنبيهات بصمت. سجلات Resend لو احتيجت لاحقًا: DKIM `resend._domainkey` (TXT) · SPF `send` (MX `feedback-smtp.us-east-1.amazonses.com` pri 10) · SPF `send` (TXT `v=spf1 include:amazonses.com ~all`).

### ب. ~~توقيع الطلبات الصادرة~~ — مفعّل ✅ (2026-08-27)
`OUTBOUND_SIGNING_SECRET` مضبوط في Vercel (Production). نقطة `/api/project-tools` ترفض أي طلب بلا توقيع HMAC صحيح (تحقق: `whoami` غير موقّع → 401 على الإنتاج). أُضيف أيضًا `CRON_SECRET` (كان ناقصًا — الفحص الصحي المجدول كان يُرفض) و `TOKEN_PEPPER`.

### ج. ~~`Demo Project`~~ — محذوف ✅ (2026-08-27)

### د. ~~مفتاح Groq مكشوف~~ — مُدوّر ومُلغى ✅ (2026-08-27)
`GROQ_API_KEY` في Vercel جديد، والمفتاح القديم حُذف من لوحة Groq.

### هـ. Migrations `0014`/`0015`/`0016` غير مُطبَّقة على الإنتاج (2026-09-12، تحديث 2026-09-13)
بوابة السياسة، طابور الموافقة (`0014`/`0015`) وسجل `agent_runs` (`0016`) موجودون في الكود ومُختبَرون بالمحاكاة فقط. بلا هذه الملفات مُطبَّقة: `agent_logs.decision`/`actor_type` غير موجودين، `tool_approvals` غير موجود (أي أداة كتابة ستفشل)، و`recordAgentRun` سيفشل بصمت (fire-and-forget) فلن تُسجَّل أي تكلفة. **طبّق الثلاثة بالترتيب قبل أي نشر يستخدم هذا الكود.**

---

## 3. الخطة — بالأولوية

### ~~أولوية 1: واجهة تحرير الوكلاء~~ ✅ (commit `a5932fc`)
لوحة «وكلاء الأقسام» في `/dashboard/settings` (مشرف فقط) لتحرير `system_prompt` و `model` لكل قسم. `requireAdmin`، وتسجيل `update_department_agent` في `agent_logs`.
**تحقق متبقٍّ:** عدّل برومبت Dev Agent من الواجهة، فوّض له مهمة، ولاحظ تغيّر سلوكه.

### ~~أولوية 2: إعطاء الوكلاء شيئًا يشغّلونه~~ ✅ (commit `6783e2a`)
نقطة `‎/api/project-tools` داخل الريبو: تحقّق توقيع HMAC + نافذة إعادة تشغيل 5 دقائق + أدوات `ping`/`echo`/`whoami`. مسجّلة كـ `mcp_endpoint` على `conecter-app` و `EDITOR.AI` و `Englisch-app`. `call_project_tool` صار يعمل من طرف إلى طرف (مُختبر على الإنتاج). أي مشروع حقيقي ينسخ `src/app/api/project-tools/route.ts` في كوده.
**تحقق متبقٍّ:** من صفحة قسم، زر «اختبار» على أداة، أو فوّض للوكيل مهمة يستدعي فيها أداة المشروع.

### ~~أولوية 3: وكلاء مجدولة (المهام الدائمة)~~ ✅ (migration `0012`)
- عمودان على `departments`: `standing_task` (فارغ = لا شيء) و `standing_task_enabled` (مفتاح إيقاف يحفظ النص)
- cron `‎/api/agent/standing-tasks` يوميًا 07:00 UTC — نفس مصادقة `daily-check` (سرّ cron أو جلسة مالك)
- يعيد استخدام `runDepartmentAgent` كما هي
- **السقف على 3 جهات:** المفتاح المُفعّل · حارس تشغيلة-واحدة-يوميًا (`ranOnDay`) · `MAX_STEPS=6` داخل المنفّذ. وسقف `MAX_DEPARTMENTS_PER_RUN=10`
- التحرير من نفس لوحة «وكلاء الأقسام» في `/dashboard/settings`
- **تحقق متبقٍّ:** اضبط مهمة دائمة لقسم من الواجهة، ثم `GET /api/agent/standing-tasks` بجلسة المالك، ولاحظ ظهور `standing_task` في البث

### ~~أولوية 4: تفعيل تسليم البريد~~ — القرار: عنوان Resend الجاهز (2026-08-28)
الدومين المخصّص مُتجاهَل (انظر §2.أ). البريد يُسلَّم لصاحب حساب Resend عبر `onboarding@resend.dev`. الوحيد المتبقّي: التأكد أن `OWNER_EMAILS` في Vercel = إيميل حساب Resend.

### أولوية 5: تنظيف
- ~~حذف `Demo Project`~~ ✅ (2026-08-27)
- ~~ضبط `OUTBOUND_SIGNING_SECRET` + `CRON_SECRET` + `TOKEN_PEPPER` في Vercel~~ ✅ (2026-08-27)
- ~~إلغاء مفتاح Groq القديم~~ ✅ (2026-08-27)
- **Sentry**: ✅ يعمل ومُثبت بالدليل. `@sentry/nextjs@10` مُوصّل بالكامل (`instrumentation.ts` · `instrumentation-client.ts` · `sentry.server/edge.config.ts` · `withSentryConfig` · `onRequestError` · `global-error.tsx` · `errors.ts` → `Sentry.captureException`). أخطاء + tracing + وسم الإصدارات (release = commit SHA)، بلا Replay (كونسول داخلي بـ CSP صارم). Next 16 + Turbopack. `/monitoring` مستثنى من `proxy.ts`.
  - مشروع Sentry: `alking-enterprises/javascript-nextjs` (US) · `SENTRY_DSN` · `NEXT_PUBLIC_SENTRY_DSN` · `SENTRY_ORG` · `SENTRY_PROJECT` في Vercel (Production + Preview)
  - **الدليل**: خطأ حقيقي من الإنتاج وصل Sentry (`JAVASCRIPT-NEXTJS-1`، وُسم بالإصدار والـ trace، ثم حُلّ). مسار التحقق المؤقت `/api/sentry-check` حُذف
  - **فخ Turbopack**: مُعالِج route عادي لا يُغسَل تلقائيًا — `Sentry.captureException` في route handler يحتاج `await Sentry.flush()` صراحةً وإلا تُفقد الأحداث قبل تجميد الـ lambda. المسار التلقائي (`onRequestError` للأخطاء غير المُلتقَطة) يغسل من نفسه
  - **يبقى (harden، اختياري):** `SENTRY_AUTH_TOKEN` لرفع source maps — بدونه الـ stacktrace في الإنتاج مُصغَّر. من Sentry → Settings → Auth Tokens

### مراجعة شاملة + تقوية (2026-08-28)
مراجعة ٣ محاور (أمن، اختبارات، منتج). التقرير الكامل في `.claude/plans/robust-puzzling-flurry.md`. المُنفَّذ:
- **`45f5888`** — **ثغرة أمنية مُصلَحة**: الوكيل المجدول كان يعمل يوميًا بلا إشراف بصلاحيات كتابة كاملة (حذف مشاريع، حقن نقاط نهاية). الآن `mode: "autonomous"` يقيّده على أدوات القراءة + `call_project_tool` فقط؛ التفويض من الكونسول يبقى كاملًا. + اختبار `tool-schemas` يقفل انحدار `null` من Groq + أخطاء `activity.ts`/MCP تذهب لـ Sentry
- **`a552623`** — تقوية SSRF (صيغ IP الرقمية، mapped-v6 hex، رفض الاتصال غير المثبَّت) · CSRF على مسارات cron عبر `Sec-Fetch-Site` (`src/lib/cron-auth.ts` موحَّد) · `REQUIRE_2FA` (بوابة اختيارية للكتابة) · قواعد rate-limit لـ `/api/agent`·`/api/maintenance`·`/api/activity/export`
- **`9c06030`** — اختبارات أفعال المالك الأساسية (تفويض، ملكية، SSRF، تسجيل)
- **بنود التقرير كلها مُنجَزة الآن:** `buildToolSet` (1.2، ضمن commit `c801b35`) · `agent_runs` (1.4، commit `fbe6e7e`) · `ToolDefinition` عام (0.4) وسطح المهام الدائمة وبريد الملخّص (2.3 + 2.4) و`/dashboard/insights` (commit `5148a6a`). التفاصيل في §1.
- **متبقٍّ لك لا للكود:** تنظيف `.env.local`/Vercel (`AI_GATEWAY_API_KEY`, `CEO_CONSOLE_MODEL`) — انظر §5.
- **طبقة التكاملات الخارجية (commit `d61e822`) — غير موثّقة هنا بعد**: هذا الملف لم يُحدَّث بعد ذلك الـcommit، الذي سبق هذه الجلسة. التفاصيل في `INTEGRATIONS.md` ولوحة `/dashboard/settings/integrations`. يحتاج دمجًا في هذه الوثيقة إن كان لا يزال قيد التطوير.

---

## 4. معرفة مهمة لا تُفقد

هذه أشياء اكتُشفت بالتجربة وتوفّر وقتًا كبيرًا لاحقًا:

### المزوّدون — ثلاثة جُرِّبوا
| المزوّد | النتيجة |
|---|---|
| Vercel AI Gateway | ✗ يرفض كل طلب بلا بطاقة ائتمان (`customer_verification_required`) |
| Google Gemini | ✗ المفاتيح المتاحة كانت رموز `AQ.` مؤقتة تموت خلال دقائق، ومشروع بحصة صفر |
| **Groq** | ✅ مفتاح دائم، بلا بطاقة، واستدعاء أدوات يعمل |

النموذج الافتراضي `openai/gpt-oss-120b` — اختير لقدرته على استدعاء الأدوات تحديدًا (الكونسول يشغّل 14 أداة).

### فخ حقيقي: التحقق الصارم من مخطط الأدوات
Groq يرفض معاملات الأدوات إن لم تطابق المخطط بدقة، **والنموذج يرسل `null` لا `undefined`** للحقول الاختيارية. كل `.optional()` كانت تنفجر:
```
`/category`: expected string, but got null
```
الحل: `.nullish()` في كل مكان. **الاختبارات المحاكاة الـ167 كانت تمر في الحالتين** — لأنها لا تمر بتحقق المزوّد.

**الدرس:** لأي ميزة تعتمد على نموذج، شغّل اختبارًا حيًا مقابل النموذج والقاعدة الحقيقيين. المحاكاة لا تكشف هذه الفئة من الأخطاء.

### فخ حقيقي: اقتباس في متغيّر بيئة يقفل المالك خارجًا (2026-08-28)
`OWNER_EMAILS` في Vercel كان محفوظًا كـ `"zwnt45602@gmail.com"` — بعلامتَي اقتباس (يحدث عند نسخ القيمة من سطر `.env`). النتيجة: الدخول ينجح (كلمة المرور صحيحة، جلسة تُنشأ)، ثم `resolveRole` يفشل لأن `isOwnerEmail` قارن مع الاقتباس، فيُرمى المستخدم لـ `/sign-in?denied=1` برسالة «غير مصرّح». وجدول `members` فارغ فلا بديل.
**الحل:** `ownerEmails()` صار يجرّد الاقتباس المحيط (commit `91156f0`) + أُعيد ضبط المتغيّر نظيفًا.
**الدرس:** رسالة «غير مصرّح» بعد كلمة مرور صحيحة = مشكلة في `OWNER_EMAILS` أو `members`، لا في المصادقة نفسها.

### تفاصيل تقنية تُنسى
- `delegationDepth` في `ToolContext`: الكونسول 0، وكيل القسم 1، والأداة ترفض عند 1. بدونه: حلقة تفويض لا نهائية تحرق رصيدًا
- `"CEO Console"` في سجل النشاط **مصدران**: الذكاء الاصطناعي، و`OWNER_ACTOR` في `src/app/actions.ts:22` لأفعالك اليدوية. هذا خدعني أثناء التشخيص
- `.agents/skills` مستثناة من eslint — ملف مهارة خارجية يستخدم `require()` وكان يُفشل CI
- زر حذف المشروع يستخدم `window.confirm` — يجمّد أتمتة المتصفح، فالحذف يدوي
- `ctx.actorType` في `ToolContext` اختياري ولا يُفرَض تلقائيًا — كل موقع بناء `ToolContext` (الكونسول، `department-agent.ts`، ناقلا MCP) مسؤول عن ضبطه بنفسه. أداة جديدة تبني `ToolContext` يدويًا وتنسى `actorType` لا تفشل (النوع اختياري) لكن `evaluatePolicy` تعاملها كأي فاعل آخر غير `standing_task_routine` — أي القاعدة الدفاعية للمهمة اليومية لن تنطبق عليها
- الموافقة على طلب مُعلَّق (`approveToolCall`) **لا تُعيد تقييم السياسة** — تنفّذ `tool.execute` مباشرة بنفس `args`/`ctx` المحفوظين وقت الطلب. إن تغيّرت صلاحيات صاحب التوكن بين وقت الطلب والموافقة، التنفيذ يستخدم السياق القديم لا الجديد

---

## 5. أول شيء غدًا

**قائمة البنود البرمجية فارغة.** كل ما كان في تقرير المراجعة
(`.claude/plans/robust-puzzling-flurry.md`) مُنجَز: 0.4، 1.2، 1.4، 2.3، 2.4،
و`/dashboard/insights`. ما تبقّى كله **يحتاج وصولًا لا يملكه وكيل برمجي** —
قاعدة بيانات حية، لوحة Vercel، أو عينك على الواجهة.

### أولوية صفر — طبّق الـmigrations المعلّقة
بالترتيب: `0014_policy_decisions.sql` → `0015_tool_approvals.sql` →
`0016_agent_runs.sql` (§2.هـ). **كل ما بُني في جلستَي 2026-09-12/13 معطّل
فعليًا حتى تفعل هذا** — الأعمدة والجداول التي يكتب فيها الكود غير موجودة بعد.

### أولوية 1 — تحقّق يدوي (بعد الـmigrations)
| ماذا | المتوقّع |
|---|---|
| توكن كتابة → `delete_project` عبر `/api/mcp` | «بانتظار الموافقة»، لا حذف؛ يظهر في `/dashboard/access` |
| موافقة مشرف على الطلب | الحذف يقع فعلًا، السجل → `success` |
| رفض طلب آخر | المشروع يبقى، السجل → `failed` |
| محادثة كونسول + تفويض حقيقي | صفوف في `agent_runs` بـ`kind` صحيح وتوكنات غير صفرية |
| فتح `/dashboard/insights` | الأرقام تطابق ما في `agent_runs` |
| صفحة قسم له مهمة دائمة | بطاقة «آخر تشغيلة» تعرض نصًا مقروءًا لا JSON |

### أولوية 2 — تنظيف في Vercel / `.env.local`
- احذف `AI_GATEWAY_API_KEY` و `CEO_CONSOLE_MODEL` — متغيّرات ميتة، لا مرجع في `src/`
- سطر `GROQ_API_KEY` مكرر في `.env.local`
- تأكّد أن `OWNER_EMAILS` = إيميل حساب Resend (§2.أ) — بريد الملخّص اليومي الجديد يعتمد عليه أيضًا
- `SENTRY_AUTH_TOKEN` (اختياري) — source maps مقروءة في الإنتاج

### ثم: ما الذي يستحق البناء بعد ذلك؟
لا يوجد backlog مكتوب. المرشّحون الطبيعيون من واقع ما بُني:
- **دمج `feat/integrations-system` في `main`** — الفرع يحمل الآن خمسة commits لم تصل الإنتاج
- **توثيق طبقة التكاملات في هذه الوثيقة** (§3) — `INTEGRATIONS.md` قائم لكن هذه الوثيقة لا تعرفه
- **ميزانية على `agent_runs`** — الجدول يقيس الإنفاق الآن؛ الخطوة التالية أن يوقف تجاوزه، لا أن يعرضه فقط

### تحقّقات يدوية معلّقة
- اضبط `standing_task` لقسم من `/dashboard/settings` وشغّل `‎/api/agent/standing-tasks` وتأكد أن الوكيل التلقائي ما استدعى إلا أدوات قراءة
- عدّل برومبت Dev Agent من الواجهة، فوّض له مهمة، ولاحظ تغيّر سلوكه
- (بعد تطبيق `0014`/`0015`) توكن كتابة يستدعي `delete_project` → يظهر «بانتظار الموافقة» لا حذفًا فوريًا، ويظهر في لوحة `/dashboard/access`
- وافق على طلب معلّق كمشرف → يُنفَّذ الحذف فعليًا ويتحول السجل لـ `success`؛ ارفض طلبًا آخر → المشروع يبقى والسجل `failed`
- (بعد تطبيق `0016`) محادثة كونسول حقيقية ثم `select * from agent_runs order by created_at desc limit 1` → صف `kind='console'` بتوكنات وخطوات غير صفرية
- فوّض مهمة لقسم من الكونسول، ثم تأكد من صف `kind='delegation'`؛ شغّل `standing-tasks` يدويًا وتأكد من صف `kind='standing_task'`
